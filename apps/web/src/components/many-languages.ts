// The count past which a per-language control becomes a wall on a phone
// (§9.3): about two rows of segments, measured on the projects Corpus
// has been tried on. The language bar becomes a picker here, and the
// state chips become a count per state.
export const MANY_LANGUAGES = 12;
