// @corpus/contract — the versioned snapshot schema (spec §4). Pure: no I/O.
// The zod schema arrives with M1; this stub pins the contract identifier.
export const CONTRACT_VERSION = "corpus/1" as const;
